# 贡献指南

感谢你愿意改进 RT-ResearchFlow。项目优先接受边界清楚、可以复现并且能够验证的改动。

## 先选择正确入口

- 可复现 Bug、安全问题之外的确定开发任务：Issue。
- 安装和使用问题、功能想法、工作流讨论：Discussions。
- 漏洞、凭据暴露或其他敏感问题：仓库 Security 页面的私密漏洞报告。

较大的功能或架构调整，请先在 Ideas 中说明用户问题、建议范围和替代方案，达成方向共识后再提交代码。不要用公开 Issue 或 Discussion 上传数据库、日志、API Key、Token、真实持仓及个人路径。

## 本地开发

要求：Node.js 20.x、pnpm 10.x，以及能够构建 Electron 原生依赖的 Windows 开发环境。

```powershell
pnpm install --frozen-lockfile
pnpm dev
```

首次启动不要求配置 AI 或 Tushare。涉及外部服务的改动应同时验证零 Key 和来源失败路径，不要把个人凭据写进测试、截图或提交记录。

## 变更原则

- 保持本地优先、证据可追溯、用户显式授权联网的产品边界。
- 不增加荐股、收益承诺、自动下单、仓位控制或规避风险提示的能力。
- Renderer 不直接持有凭据、数据库连接或任意网络权限；新增能力应通过窄 IPC 和主进程校验。
- 数据库结构变化必须新增向前 Migration，覆盖旧库升级、幂等和失败行为。
- 不修改历史研究账本来伪造新结果；需要修订时应保留来源、时间和前序关系。
- 复用现有类型、服务和 UI 组件，避免与当前架构无关的重构。

## 验证要求

提交 Pull Request 前至少运行：

```powershell
pnpm run verify
./.github/scripts/Test-PublicBoundary.ps1
```

`verify` 包含 Node/Web TypeScript、ESLint、单元测试和生产构建。涉及真实交互时，请增加或更新相应的 Playwright 旅程；涉及时间、网络或 AI 时，测试必须可重复，不能依赖付费调用或公网瞬时状态。

界面改动还应检查默认 1680x960 窗口、最大化、亮暗主题、键盘焦点、减少动态效果和文本溢出。固定窗口不等于固定内容尺寸，最大化后的布局仍需保持可用。

## 提交与 Pull Request

- 一个 Pull Request 只解决一个清晰问题。
- 说明用户可见结果、技术边界、验证证据和已知限制。
- 新增依赖时说明为什么现有依赖或平台能力不能满足需求。
- 不提交构建产物、运行数据库、日志、测试报告或临时配置。
- 接受 Review 后再修改原提交或追加提交；不要重写其他贡献者的历史。

项目使用 AGPL-3.0-only。提交代码即表示你有权提供该贡献，并同意其按仓库许可证发布。
