# 安全策略

RT-ResearchFlow 会处理本地研究数据、数据源凭据和 AI Provider 凭据。请把可能造成凭据泄露、任意网络访问、任意文件访问、数据库破坏、权限绕过或不可信内容执行的问题视为安全问题。

## 支持范围

| 版本 | 安全修复 |
| --- | --- |
| 最新公开测试版 | 尽力支持 |
| 更早的测试版和未发布构建 | 不支持 |

在正式稳定版发布前，安全修复可能只进入新的测试版本，不承诺对旧安装包提供补丁。

## 私密报告

请使用仓库 Security 页面中的 **Report a vulnerability** 提交私密漏洞报告，不要创建公开 Issue 或 Discussion。报告应尽量包含：

- 受影响版本、运行环境和安装方式。
- 最短复现步骤、影响范围和必要的脱敏截图或日志。
- 是否需要用户交互、特定配置、联网状态或已有本地权限。
- 已知缓解方式；如有修复建议，也可以一并提供。

请勿发送真实 API Key、Token、持仓数据库或其他用户数据。可以使用测试凭据和最小化样本复现。维护者确认问题并准备修复前，请不要公开漏洞细节。

## 不属于安全漏洞

- 公开数据源短时不可用、限流、字段变化或数据延迟。
- 模型输出质量、投资判断分歧或收益不符合预期。
- 用户主动导入不准确数据后得到的错误分析。
- 已在界面明确披露且不突破权限边界的数据缺口。

这些问题可以按 Bug 或使用问题通过普通入口反馈。
