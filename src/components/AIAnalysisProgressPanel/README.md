# AI 分析全局进度面板

组件挂载在 `App` 根层，消费 `appStore.aiProgress`，保证单篇和批量 AI 分析切换版块后仍可见且不会中断后台任务。

- 展开态展示完整阶段与 Token 用量，并提供44px最小化按钮。
- 最小化只改变本地展示状态，紧凑任务条继续展示当前阶段、总体进度和44px展开按钮；后续进度事件不得强制展开。
- `done/error` 沿用2秒自动清理；清理后启动的新任务默认展开。
- 支持亮暗主题、键盘焦点和减少动态效果；不新增 IPC、持久化或 AI 调用。
